package com.education.tp.interfacekeyword;

public interface Account {
    //We declared the signatures of our methods in the interface
    void deposit(int amount);
    int withdraw(int amount);
    int getBalance();
}
